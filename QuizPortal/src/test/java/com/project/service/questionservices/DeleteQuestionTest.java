package com.project.service.questionservices;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;
/*
class DeleteQuestionTest {

    @Test
    void perform() {
        DeleteQuestion deleteQuestion=mock(DeleteQuestion.class);
        deleteQuestion.perform();
        verify(deleteQuestion,times(1)).perform();
    }

    @Test
    void setParameters() {
        DeleteQuestion deleteQuestion=mock(DeleteQuestion.class);
        deleteQuestion.setParameters();
        verify(deleteQuestion,times(1)).setParameters();
    }

    @Test
    void getToBeDeleted() {
        DeleteQuestion deleteQuestion=mock(DeleteQuestion.class);
        deleteQuestion.setToBeDeleted(1);
        when(deleteQuestion.getToBeDeleted()).thenReturn(1);
        assertEquals(1,deleteQuestion.getToBeDeleted());
    }

    @Test
    void setToBeDeleted() {
        DeleteQuestion deleteQuestion=spy(DeleteQuestion.class);
        deleteQuestion.setToBeDeleted(2);
        assertEquals(2,deleteQuestion.getToBeDeleted());
    }
}

 */